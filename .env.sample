# ===================================================================
# Azure AI Search 핸즈온 워크샵 환경설정 파일
# ===================================================================
# 이 파일을 복사하여 .env 파일을 만들고 실제 값을 입력하세요.
#
# Windows:   copy .env.sample .env
# macOS:     cp .env.sample .env
# Linux:     cp .env.sample .env
#
# ⚠️ 주의: .env 파일은 절대 Git에 커밋하지 마세요! (.gitignore에 포함됨)
# ===================================================================

# -------------------------------------------------------------------
# Azure AI Search 설정
# -------------------------------------------------------------------
# Azure Portal → Azure AI Search 리소스 → 개요 페이지에서 확인
SEARCH_ENDPOINT=https://your-search-resource.search.windows.net

# Azure Portal → Azure AI Search 리소스 → 키 페이지에서 "기본 관리 키" 복사
SEARCH_ADMIN_KEY=your-search-admin-key-here

# 인덱스 이름 (변경하지 마세요)
SEARCH_INDEX_NAME=products-index

# -------------------------------------------------------------------
# Azure OpenAI 설정
# -------------------------------------------------------------------
# Azure Portal → Azure OpenAI 리소스 → 키 및 엔드포인트에서 확인
# 또는 Azure AI Studio (https://ai.azure.com)에서 확인
FOUNDRY_PROJECT_ENDPOINT=https://your-openai-resource.openai.azure.com/

# Azure Portal → Azure OpenAI 리소스 → 키 및 엔드포인트에서 "KEY 1" 복사
FOUNDRY_PROJECT_KEY=your-openai-api-key-here

# -------------------------------------------------------------------
# 모델 배포 이름
# -------------------------------------------------------------------
# Azure AI Studio → 배포 탭에서 확인
# 아래 이름으로 배포했다면 변경하지 마세요.
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-small
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o-mini

# API 버전 (변경하지 마세요)
AZURE_OPENAI_API_VERSION=2024-06-01


# -------------------------------------------------------------------
# JIRA API 설정
# -------------------------------------------------------------------
# JIRA Cloud 인스턴스 URL
# 예: https://your-domain.atlassian.net
JIRA_SERVER_URL=https://your-domain.atlassian.net

# JIRA 사용자 이메일
JIRA_USER_EMAIL=your-email@example.com

# JIRA API 토큰 (Personal Access Token)
# https://id.atlassian.com/manage-profile/security/api-tokens에서 생성
JIRA_API_TOKEN=your-jira-api-token-here

# 기본 프로젝트 키 (선택사항)
# API 호출 시 project_key를 명시적으로 전달할 수 있습니다.
# 예: PROJ, DEV, OPS
JIRA_PROJECT_KEY=your-project-key

# -------------------------------------------------------------------
# GitHub API 설정
# -------------------------------------------------------------------
# GitHub 개인 액세스 토큰 (Personal Access Token)
# https://github.com/settings/personal-access-tokens에서 생성
# 필요한 권한: repo (Full control of private repositories)
GITHUB_TOKEN=your-github-personal-access-token-here

# 기본 저장소 이름
# 예: user/repository
GITHUB_REPO_NAME=your-repository-name


# -------------------------------------------------------------------
# 📧 Google API (Gmail) 설정
# -------------------------------------------------------------------
# 💡 경로 입력 팁: 
# 1. 절대 경로 권장: {your_path}\project\credentials\token.json
# 2. 프로젝트 루트 기준 상대 경로 가능: ./credentials/token.json
GMAIL_TOKEN_PATH={your-path}\ms-aitour\credentials\mail_token.json
GTASK_TOKEN_PATH={your-path}\ms-aitour\credentials\task_token.json
GOOGLE_CREDENTIALS_PATH={your-path}\ms-aitour\credentials\credentials.json